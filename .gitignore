node_modules
target
src-tauri/target
src-tauri/gen
# Emitted by some Vue tooling alongside .vue files — regenerated on demand
src/**/*.vue.js
dist

# VitePress site build artefacts (the source lives in website/, the
# generated bundle is regenerated by the Pages workflow on every push)
website/.vitepress/dist
website/.vitepress/cache
